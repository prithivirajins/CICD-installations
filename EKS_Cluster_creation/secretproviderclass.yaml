apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: jenkins-secret
  namespace: default
spec:
  provider: aws
  parameters:
    region: "ap-south-1"
    objects: |
      - objectName: "jenkinsENVvariables"
        objectType: "secretsmanager"
